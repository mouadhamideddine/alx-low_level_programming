#include "main.h"
/**
 * print_numbers - from 0 to 9
 * description : look up
 * Return: None
 */

void print_numbers(void)
{
	int count;

	for (count = 0; count < 10; count++)
	{
		_putchar(count + '0');
	}
}
